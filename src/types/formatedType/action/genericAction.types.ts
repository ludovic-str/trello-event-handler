export interface Board {
  id: string;
  name: string;
  shortLink: string;
}

export interface Organisation {
  id: string;
  name: string;
}

export interface Card {
  id: string;
  name: string;
}

export interface List {
  id: string;
  name: string;
}
